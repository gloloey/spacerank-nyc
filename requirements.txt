# SpaceRank NYC — RUNTIME dependencies (what the deployed app needs).
# Scraper/dev extras live in requirements-dev.txt.
fastapi
uvicorn
pandas
numpy
# query-time embedding encoder for the deployed semantic backend (~40 MB, no PyTorch):
onnxruntime
tokenizers
# optional, LOCAL ONLY — full-precision embeddings via PyTorch (~2 GB):
# sentence-transformers
