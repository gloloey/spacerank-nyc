# SpaceRank NYC — development / scraping / precompute extras
-r requirements.txt
requests
beautifulsoup4
lxml
# real embeddings locally + the offline precompute tool:
sentence-transformers
huggingface_hub
httpx
python-dotenv   # loads .env.local for db.py when running/testing locally
